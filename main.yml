---
- name: Gather the state of the Magisystem
  hosts: edge
  gather_facts: false
  roles:
    - state_gathering

- name: Setting up the Magisystem
  hosts: edge
  gather_facts: false
  pre_tasks:
  roles:
    - role: edge/install
      when: os_is_installed is falsy(convert_bool=True)
    - role: edge/containers
